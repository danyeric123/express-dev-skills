<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Express To-Do</title>
</head>
<body>
	<h1>Dev Skills</h1>
  <% if(error) { %>
    <h2><%= error %></h2>
  <% } else { %>
    <h2>Dev Skill #<%= devSkill._id %></h1>
    <h3><%= devSkill.text %></h3>
    <h3><%= devSkill.proficiency ? 'Proficient' : 'Novice' %></h3>
    <form 
            action="/devSkills/<%= devSkill._id %>?_method=DELETE"
            class="delete-form"
            method="POST"
          >
    <button type="submit">Delete Skill</button>
  <% } %>
	<a href="/devSkills">Go back</a>
</body>
</html>